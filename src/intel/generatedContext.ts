// AUTO-GENERATED by scripts/gen-context.js from the deep service-by-service audit corpus.
// Do NOT edit by hand. Source: engine-context-corpus-2026-06-14.json. Re-run: node scripts/gen-context.js
/* eslint-disable */

export const IMPROVED_DOSSIER: string = "## SERVITIUM_DOSSIER (replacement — maximal, factual, auto-refreshable)\n\n### What Servitium is (one paragraph)\nServitium is a freemium DEEP-ADMINISTRATION SaaS for survival/RCON game-server COMMUNITIES — today Conan Exiles + Soulmask, plus a free Discord-only community tier. It is NOT a server hoster/reseller and not a generic monitor; the value is deep admin + community tooling (in-game shop/economy, 0%-commission donations, raid protection, clan wars, bounty hunt, wipe management, live tactical map, quests) enforced in-game via a cross-platform agent. One paid tier (Pro, 9.99 EUR/mo per game server, Stripe recurring) monetizes automation/pro-tooling; a separate free Discord product (discord.servitium.org) is top-of-funnel advertising. BattleMetrics is a reference for what generic tools LACK (no shop, no economy, no 0% donations), never a head-on competitor.\n\n### The repos (7 apps + shared; CLAUDE.md says \"5\" — that is stale)\n1. servitium-api — NestJS + Mongoose + MongoDB + Socket.IO. The unified backend (auth, RBAC, economy, RCON pool, Discord bot lifecycle, billing, migrations, WS gateways). PM2 user `servitium`, https://api.servitium.org. tsconfig NON-strict.\n2. servitium-center — Angular 20 admin panel (standalone, signals, @if/@for, inject(), localize pipe, 6 langs, ~55 core services). The owner control plane. tsconfig strict.\n3. servitium-portal — Angular 20 player site (SSR + prerender, 6 lang URL prefixes). Public pages + per-server sub-app /s/:slug. tsconfig strict. ZERO test harness.\n4. servitium-ui — multi-library Angular workspace (ng-packagr): @servitium/ui (svt-* design system) + @servitium/discord (shared Discord widgets + DI ports). Pure UI/types/ports, no API/WS calls. Consumed via local tgz.\n5. servitium-electron-gui — headless Electron (NO window) + Rust NAPI addon (game_db_reader.node). Installed on every game host (Windows AND Linux). Logs in, registers Host, opens a WS keyed by gameInstanceId, is fully piloted from Center. tsconfig strict.\n6. servitium-discord — standalone Angular 20 SPA at discord.servitium.org. The FREE, agent-free Discord product (welcome/goodbye embeds + Join-to-Create voice). Runtime i18n dict (no localize build). Karma/Jasmine.\n7. servitium-autodev — private owner-only automation: a BUILD lane (auto-labelled GitHub issues → 17-state agent FSM → DRAFT PR, never merges) and an INTEL lane (SIE: daily veille → ranked opportunities → Opus feasibility briefs with paste-ready Max prompts). THIS engine. SDK pinned @anthropic-ai/claude-agent-sdk@0.3.177. Dedicated Console key, ~100 EUR/mo cap.\nPlus `shared/` — leaf type package (WS event enums + payloads, entitlements matrix, game-type enum, api-dtos, conan map) imported by relative path from API + agent + frontends. No runtime code. Breaking changes land in all consumers in the same commit.\n(There is also a legacy servitium-storybook — dead, ignore.)\n\n### Two-collection domain model (the #1 footgun)\n- `servers` (Server doc) = the COMMUNITY/BRAND: name, slug, shop, donations, raid protection, wars, quests, Discord config, banner, players, wipes, `plan`('free'|'pro' — the Pro gate axis), `tier`('free'|'paid' — Discord-only vs has-game-server, DIFFERENT field), `ownerId`, `gameInstanceId`, `stripeCustomerId/stripeSubscriptionId`. ONE Server is billable.\n- `game_instances` (GameInstance doc) = the RUNTIME the agent pilots: `hostId`, ip/ports/passwords, mods, `maxPlayers`, `gameMode`, `isOnline`, `lastHeartbeat`, `sessionName`, `desiredState`, `localPaths`, `installState`.\n- 1 Server ↔ 0..1 GameInstance via `Server.gameInstanceId`. A `Host` (collection `hosts`, fingerprint UUID from agent host.json) is the physical VPS.\n- THE AGENT WS ROOM IS KEYED BY `gameInstanceId`, NOT serverId. API helpers `resolveGiId(serverId)`, `guiConnectedFor(serverId)`, `safeGuiDispatch(serverId,...)` translate community→game-instance. In the agent, `serverId`/`communityServerId` = Server._id (scopes /sync/* + /client/:id/*); `apiServerId`/`gameInstanceId` = GameInstance._id (WS handshake + RCON). The agent field NAMED `apiServerId` actually scopes the game-instance — conflating the two silently breaks either sync or the WS room.\n\n### Game-type discriminator\nCanonical enum: `shared/enums/game-type.enum.ts` = CONAN_EXILES | SOULMASK | DISCORD (DISCORD is intentionally skipped in game if/else chains). DUPLICATED at `servitium-electron-gui/src/shared/enums/game-type.enum.ts` (keep in lockstep). The string is hardcoded as a literal union in many files; casing is inconsistent (portal Server.model uses lowercase 'conan_exiles'/'soulmask', Center/UI use uppercase) — normalize with toUpperCase(). `guildId` is `number` for Conan, a GUID `string` for Soulmask — typed `number|string` everywhere; never parseInt without the `/^\\d+$/` branch. Two data paths: Conan = SQLite game.db watcher (Rust game_db_reader) + log parser; Soulmask = log parser ONLY (its DB schema differs). A new game without SQLite (Rust) takes the Soulmask-style log/RCON-only path.\n\n### Commercial model (LIVE — the dead model below must NOT be resurfaced)\n- Single source of truth for gating: `shared/entitlements/index.ts` — `Entitlement` enum (27 keys) + `ENTITLEMENT_MIN_PLAN`; ALL keys currently require 'pro'. Anything NOT in the enum is FREE. Gate axis = `Server.plan`. API enforces via `EntitlementGuard` + `@RequiresEntitlement(...)`; denial = 403 `{code:'UPGRADE_REQUIRED', entitlement}`; SUPERADMIN bypasses; no-decorator route = free.\n- ONE paid tier: Pro, 9.99 EUR/mo, billed per game server, Stripe recurring (`STRIPE_PRICE_ID`). `POST /billing/servers/:id/checkout` → Stripe Checkout (mode subscription, metadata.serverId). `POST /billing/webhook` (raw body, signature-verified) flips `Server.plan` pro on `checkout.session.completed`, free on `customer.subscription.deleted`. Manual override: `PATCH /servers/:id/entitlements/plan` (superadmin).\n- FREE (the acquisition funnel, generous on purpose): 1 server (FREE_SERVER_LIMIT=1), players + web kick/ban, FULL economy/shop incl. MANUAL donation crediting, raid-protection ALERT (detection only), ALL Discord, ALL live map incl. raid alerts, team mgmt, tickets, restart scheduler, templates, hosts mgmt, MANUAL wipe + season history.\n- PRO (what makes the admin money / saves time / is \"pro\"): multi-server; VPN+multi-account detection; economy wildcards/admin-packs, CASHBACK withdraw, advanced stats, shop alerts, DONATION AUTO-CREDIT via PayPal; raid TP-prison enforcement + raid stats; wipe automation + seasons; RCON + live chat (100% Pro); the ENTIRE Features block (war declaration, bounty hunt, quests, events, treasure hunt, boss hunt, raid alerts); Conan auto-exchange + server-features; audit log.\n- The four monetization levers: (a) MANUAL-DONATION FRICTION = the deliberate upsell — free admins credit donations by hand via a ticket; Pro auto-credits PayPal→wallet. The PayPal capture path is gated `@RequiresEntitlement(EconomyDonationAutocredit)`; the manual endpoint is only role-guarded. (b) 0% COMMISSION ALWAYS — Servitium never touches donation money (admin's own PayPal), positioned vs Tebex ~5%; also a legal shield (not a payment intermediary). (c) CASHBACK (3-bucket wallet: balance/donationBalance/cashback; withdraw is the gated Pro action). (d) MULTI-SERVER.\n- Stripe = platform Pro subscription ONLY. PayPal = player donations ONLY (admin's own). Two rails that never mix.\n- The Discord product: free bot, free-vs-paid split = \"needs the game agent or not\". Free (agent-free): welcome, voice hubs (done), backlog auto-role/reaction-roles/moderation/leveling/giveaways/announcements/stats-voice/translator, tickets LAST. Paid (agent-bound): killfeed, pvp-stats, server-status, building alerts, raid protection, wars, bounty, whitelist, shop, admin gifts. Reuses the Server doc with GameType.DISCORD + tier:'free'; free→paid = a flag flip (link a GameInstance) with ZERO migration. Upgrade path = the Center (Discord product has no signup of its own).\n\n### DEAD commercial model (do NOT resurface)\nThe OLD multi-plan subscription system AND the separately-sold anti-DDoS add-on (was 15 EUR/mo) + API platform (was 30 EUR/mo) are OBSOLETE (~39 files flagged for removal in FREEMIUM-PLAN.md). The `subscriptions` module + `SubscriptionFeature.DDOS_PROTECTION` are legacy/parallel to entitlements. The older `project_features_positioning` memory still cites 15/30 EUR — superseded by the 2026-06-12 freemium decision. Anti-DDoS infra remains a trade secret never named in user-facing copy.\n\n### The technical moat (5 compounding layers)\n1. Rust game_db_reader (servitium-electron-gui/src/rust-modules/game-db-reader/, lib.rs ~2255 + restructure_v3.rs + restructure_v2.rs): a napi cdylib that opens Conan's live SQLite with sqlx over a shared Arc<RwLock<SqlitePool>> tokio pool. It reverse-engineered the Funcom schema (account/characters/guilds/buildings/building_instances/buildable_health/actor_position/game_events), hand-decoded ~50 numeric event types (86 ThrallKill, 87/88 building destroyed, 103 CharacterDeath=PvP kill, 171-179 decay/purge/lootbag), streams incrementally via query_new_events(last_row_id,limit) with a 10-way enrich join, and has anti-cheat SQL: a LAG() window query discarding same-guild kills + kill-spam within 600s. Conan-only; Soulmask/Rust have no equivalent reader. THIS is the core moat.\n2. Dual-stack patched RCON: Conan via rcon-client (TCP Source RCON), Soulmask via rcon-srcds + a bespoke interactive-paginated raw-TCP walker for lp/lap (sends 'n'/'q', strips ASCII frames). The patches/rcon-client+4.2.5.patch (applied via patch-package postinstall in BOTH api + electron-gui) fixes an upstream off-by-one packet-id bug (Timeout for packet id 1 = RCON dead without it), raises maxPending 1→1000, adds failedQueue retry + 6-min ECONNRESET reconnect. Circuit breaker: 3 fails → open 5min (doubles to 1h cap).\n3. Shared Discord lib (@servitium/discord) via ports/adapters: 3 components depend only on DISCORD_DATA_PORT/TRANSLATE_PORT/NOTIFICATION_PORT; Center and the standalone app each bind their own adapter → identical UI/behaviour, zero duplication, with paid agent-only methods explicitly fenced at the bottom of the 89-line port.\n4. Cross-platform headless agent: same binary semantics on Windows + Linux (xvfb-run wrap on Linux). No .exe/WindowsServer/Win64 hardcodes — platform.ts helpers + path-resolver.ts derive every path from ONE user-picked folder via ordered PATH_CANDIDATES, so raw filenames never leak.\n5. WS real-time control plane keyed by gameInstanceId: agent→API metrics/status/crash/bounty-position/command-response; API→agent commands (start/stop/restart, the wipe family, war/protection updates, bounty fast-track) with request/response correlation (commandId + 30s timeout). 60s heartbeat (offline at 180s) + a sync orchestrator (light/full) both driven by the one shared Rust reader.\n\n### Roadmap / direction (what the owner WANTS — score opportunities against this)\n- MORE GAMES: Rust is the priority candidate, then ARK and other survival/RCON titles. (See newGamePlaybook — adding a game touches 6 of 7 repos; it is the most cross-cutting change in the codebase.)\n- Refactor/optimize/secure the API; harmonize CSS across Center/UI; close i18n gaps (6 langs); finish + grow the Discord product; explore Discord-bot monetization; possibly new business lines.\n- \"Improve AutoDev itself\" is a FIRST-CLASS opportunity kind: smarter/broader intelligence, higher-quality briefs, new dashboard views/metrics, deeper self-improvement.\n\n### Conventions (respect in every proposal/brief)\n6 languages (en/fr/de/es/pt/ru), every user-visible string through the localize pipe + keys in all 6 files AT CREATION; no emoji in UI (svt-icon/lucide); never expose internal infra (OVH, OPNsense, WireGuard, PM2, nginx, vRack) or raw filenames (game_0.db, ConanSandbox.log, Engine.ini, *.exe) in user copy; no em-dashes in copy; no browser alert/confirm/prompt (use ConfirmationModalService/svt-modal or ToastrService); comments default ZERO (one short English line max when WHY is non-obvious, never French); API change ships a green mongodb-memory-server integration test (NOTE: this is API-ONLY — Center/Portal use TestBed+HttpTestingController, Portal has no harness, Discord uses Karma); API tsconfig is non-strict, all Angular repos + electron-gui + autodev are strict; never git push on own initiative (owner pushes); deploy via deploy.ps1 (PowerShell tool, one target at a time); the rcon-client patch must stay applied; agent path code uses platform.ts/path-resolver.ts helpers.";

export const PER_APP_CONTEXT: Record<string, string> = {"servitium-api":"NestJS + Mongoose + MongoDB + Socket.IO. tsconfig NON-strict (strictNullChecks:false, noImplicitAny:false) — relying on implicit-any/nullable is fine here, unlike the Angular repos. Bootstrap src/main.ts: NestFactory rawBody:true (REQUIRED for Stripe webhook sig), trust proxy=1, helmet (crossOriginResourcePolicy cross-origin, CSP off), global ValidationPipe (whitelist+forbidNonWhitelisted+transform), /health off the guard chain, Swagger /api/docs. App wiring src/app.module.ts: ScheduleModule.forRoot() appears at lines 81 AND 161 (duplicate, harmless, do not add a third); global guards ThrottlerGuard→DemoReadOnlyGuard (APP_GUARDs) + JwtAuthGuard + RolesGuard (CommonModule @Global). Config src/config/configuration.ts: JWT_SECRET + JWT_EXPIRES_IN have NO fallback (fail-fast). Auth: httpOnly-cookie JWT + parallel Bearer; signSlim() strips servers+gameAccounts (<4KB); Discord OAuth. Guards order MATTERS: ServerRoleGuard MUST precede RoleGuard/PermissionGuard/EntitlementGuard (they read request.userServerRole). OWNER from Server.ownerId, not server_roles. Permission matrix src/common/interfaces/permission-set.interface.ts (14 feature namespaces). Entitlement matrix shared/entitlements/index.ts (27 keys, all 'pro'). WS contracts shared/ws-events/. RCON routing rcon.service.ts executeRconAuto (Soulmask→SoulmaskRconCliManager incl interactive lp/lap pager; else Conan→RconClientManager); strategy factory src/rcon/strategies/rcon-strategy.factory.ts (the src/infrastructure/rcon/* one is NOT wired). KILL = eventType 103 hardcoded in game-db-sync.service.ts processKillsFromBatch (~line 1181). Agent intake auth gap: GET /client/:serverId/watched-items + POST /client/:serverId/game-events/client only check Bearer prefix (run as userId:'system'); downloads controller has ZERO auth by design. RUNTIME_FIELD_NAMES in servers.service.ts governs splitForUpdate (a new runtime field MUST be added or PATCH /servers/:id drops it). Wipe: WipeSchedulerService cron */1, 6-step; TWO cleanupSupportingCollections impls (add new collections to BOTH). Demo: DemoReadOnlyGuard blocks non-GET for isDemo JWTs. Migrations run onApplicationBootstrap, failures swallowed → must be re-runnable from scratch. TEST RULE: every change ships a green mongodb-memory-server integration test (149 *.spec.ts; copy the nearest sibling's MongoMemoryServer setup; coverage threshold 70%; npm run test / test:ci). Deploy: deploy.ps1 -Target api (PowerShell tool). Adding a game: shared enum first, then games.service.ts catalogue entry, rcon strategy, branch processKillsFromBatch/config-general/scheduler/opnsense/game-items enum/GAME_APP_IDS.","servitium-center":"Angular 20 admin panel. tsconfig FULL STRICT (strictTemplates, noImplicitOverride, noPropertyAccessFromIndexSignature) — no `any`, templates type-check. Standalone + signals + @if/@for + inject(), no NgModules. Bootstrap src/main.ts: provideHttpClient(withInterceptors([AuthInterceptor, PagePathInterceptor, ErrorInterceptor])) — order load-bearing; SVT_TABLE_LOCALE/LOCALE_ID factories; 6 locale registration; root route '' is centerAccessGuard; every feature lazy. Root shell app.component.ts renders <router-outlet> + <svt-confirmation-modal> (must be in root shell or confirmation promises never resolve). ~55 core services in src/app/core/services/. Data-loading pattern: effect() firing loadX(serverId) on ServerService.selectedServer() change, guarded by lastLoadedServerId. WS: WebSocketService (/ws) SUBSCRIBE_SERVER on connect, room keyed by gameInstanceId (the /ws/map gateway keys by Server _id — do not conflate). Entitlements: EntitlementsService.has(key) locked-by-default, SUPERADMIN bypass, reloads on selectedServer change; route guards proGuard/proPlanGuard→/upgrade; component gates svt-pro-gate/ProLock(blur)/ProPreview(readable). Permissions: AuthService.hasPermission(feature,action); *svtHasPermission is NON-reactive (re-set on nav), *svtIsOwner is reactive. Auth: httpOnly cookie of-record + in-memory _accessToken + localStorage 'current_user'; guards call ensureAuthenticated() first (replays _bootAuth$ shareReplay) to avoid F5-logout; setTimeout(0) in initializeAuth() is load-bearing (circular-DI). RCON never issued directly — proxy via POST /rcon/:serverId/* + /rcon-chat/:serverId/*; control via POST /game-instances/:gameInstanceId/control/:action (GameInstance id). svt-* gotchas: [svtCardHeader]/[svtCardFooter] need <ng-template> host (NG0201 on <ng-container>); modal <ng-content select> breaks on multi-root @if/@else (NG8011, wrap in <ng-container>); svt-data-table row actions need BOTH [actions] AND a type:'actions' column. TEST RULE (DIFFERENT from API): TestBed + HttpTestingController (provideHttpClient + provideHttpClientTesting), NOT mongodb-memory-server; mirror core/services/*.service.spec.ts (expectOne url+method+body, flush, http.verify). i18n: localize pipe (impure) + keys in ALL 6 src/locale/messages.{en,fr,de,es,pt,ru}.json at creation. Build/deploy ALWAYS --configuration production (dev bakes localhost:3000); deploy.ps1 -Target center; manual fallback tar from INSIDE dist/servitium-center/browser. Cross-platform bug magnet: host-servers-tab.component.ts configFileOptions + server-setup-wizard.deriveDefaults() hardcode WindowsServer paths even on Linux.","servitium-portal":"Angular 20 player site, SSR + prerender, tsconfig FULL STRICT. ZERO .spec.ts and no jest/karma — the API green-test rule does NOT apply; verify by build + visual check against prod. Standalone + signals + @if/@for + inject(). Routing: app.routes.ts childRoutes defined ONCE then spread 6x for en + /fr/de/es/pt/ru (any new route goes in childRoutes); app.routes.server.ts MUST mirror the expansion or prerender misses pages — RenderMode buckets: staticPaths=Prerender, csrPaths=Client, slugPaths=Prerender (publicSlugParams), csrSlugPaths(bounty,events)=Client, authedSlugPaths(wallet,quests,my-stats)=Client. A new feature-gated slug route goes in csrSlugPaths NOT slugPaths (featureGuard reads browser-only signals; prerendering ships empty HTML). SSR i18n: provideAppInitializer pre-loads by pathname before render or SSR ships raw keys. Auth (dual, mid-migration): httpOnly refresh cookie is real auth, access token in-memory only; authInterceptor sets withCredentials:true on EVERY request + Bearer only if in-memory token present; authGuard is SYNCHRONOUS (does not await refresh — expired access mid-session bounces to login even with valid cookie). KNOWN BUGS not to regress: RegistrationModalComponent writes tokens to localStorage (the open security item); ApiService.getHeaders() reads localStorage.getItem('accessToken') which AuthService no longer stores (prefer authInterceptor). Server context: serverContextInterceptor appends X-Server-Id inside /s/:slug. Two WS namespaces (lifecycle owned by ServerLayoutComponent): WebSocketService /public (JOIN_SERVER; 'raid_alert' is a RAW string event not in the enum, registered separately) and MapWebsocketService /ws/map (AUTH-REQUIRED — silently no-ops if called before refreshToken resolves). guildId is number|string everywhere. gameType casing inconsistent (Server.model lowercase 'conan_exiles'/'soulmask') — normalize toUpperCase(); isSoulmask() is implemented 3 different ways. CoordinateConverterService NOT in barrel (eager leaflet import). UI set is portal-local src/app/shared/ui (NOT @servitium/ui); svt-panel/svt-slot/portal-modal are portal-local svt-*; svt-badge vs portal-badge distinct. i18n: localize + ldate pipes (impure), messages.{lang}.json (6); seed-faq-i18n.mjs OVERWRITES FAQ in all 6 — do not run after manual edits. Generated API types src/app/api/generated.ts is a permanent STUB; regen npm run gen:api against live localhost:3000. Deploy: deploy.ps1 -Target portal; tar from INSIDE dist/servitium-portal/browser. PushNotificationsService registers /sw.js at root (must deploy there). New game branches in only ~4 files: Server.model gameType union, CoordinateConverterService, BountyMapComponent, leaderboard column/label signals + SEO/banner copy.","servitium-ui":"Multi-library Angular 20.3 workspace (root version 0.1.4), tsconfig FULL STRICT, two ng-packagr libs: @servitium/ui (svt-* design system) + @servitium/discord (Discord widgets + 3 DI ports). PURE UI/types/ports — no API/Mongo/WS/RCON. Consumed by Center + Portal + the standalone discord app via LOCAL TGZ. REBUILD after ANY lib change: npm run build:pack (= ng build + cd dist/servitium/ui && npm pack); output dist/servitium/ui/*.tgz; IGNORE stale root tarballs 0.0.0/0.0.1.tgz; then update the tgz ref + npm install --force in consumers (bypass cache). @servitium/ui ships SCSS tokens as ASSETS (ng-package.json assets) — token changes REQUIRE rebuild+reinstall. Adding any component REQUIRES an export line in the project's public-api.ts + rebuild. NOT exported today (import by deep path or add to barrel): all 8 admin organisms + several molecule cards (SvtServerCard/PlayerStatsCard/LeaderboardItem/etc.) + iucoin. svt-icon resolution: iconMap[name]→toPascalCase→renames table (guards lucide ^0.546 renames Loader2→LoaderCircle, Edit2→SquarePen, Filter→Funnel, Grid→Grid3x3, BarChart3→ChartColumn)→Circle; do NOT bump lucide without rechecking renames. player-events-timeline.getEventCategory() HARDCODES Conan game_db_reader event-type integers (combat=[103], thrall/building/system arrays) — MUST branch for a new game or drive from a [gameType]/[eventTypes] input. svt-data-table: SVT_TABLE_LOCALE token (provide at root or English fallback); row actions need BOTH [actions] + type:'actions' column. svt-modal/select/date-pickers need CDK Overlay + provideAnimations(). svt-card header/footer directives inject TemplateRef → <ng-template> only. gameType union 'CONAN_EXILES'|'SOULMASK' duplicated across ~12 files (portal-hero, portal-server-grid, server-card, player-stats-card, leaderboard-item, user-profile-card, protection-request-form, analytics-dashboard) with matching gameTypeChip/Label/getGameIcon switches — the cross-cutting new-game extension point (portal-server-grid inline ternary silently labels unknown games 'Soulmask'). @servitium/discord: 3 components + 3 ports + discord.types.ts (ChannelTier 'free'|'agent', KillfeedOptions{showWeapon}, DiscordGameAccessMode), all game-agnostic — REUSE as-is; the only game gate is in Center's discord.component.ts host, not the lib. KNOWN BUGS to fix when adjacent: 8 organisms have `ngOnChanges(...): void : void` double-void; analytics/player-stats dashboards have empty `import {} from '@angular/common'` (no date/async pipe); French leaks in status-indicator/breadcrumb/organisms; emoji in SvtStatsCardComponent.getMedal(); dead _variables.scss + buggy _utilities/_base.scss. TEST: ng test (karma); no integration harness here. New game: add the gameType union member + a BEM SCSS rule per new variant (a union member without an SCSS rule = unstyled, no error).","servitium-electron-gui":"Headless Electron (NO window) + Rust NAPI addon. tsconfig STRICT, CommonJS (tsc -p tsconfig.main.json). Build npm run build (= build:main tsc + build:rust node scripts/build-rust.js); entry pre-init.js → dist/main/main/main.js (ServitiumMonitor). 37 *.spec.ts (jest+ts-jest, stub IO + real classes — NOT mongodb-memory-server); every change ships a green spec. Deploy (Linux AppImage ONLY, build on prod VPS — Windows can't cross-compile sharp/sqlite3/Rust): the CLAUDE.md AppImage block (tar src minus node_modules/dist/release/target → scp → npm install --legacy-peer-deps → npx patch-package AFTER install → npm run dist:linux → cp to /opt/servitium/uploads/client/v1/). rcon-client patch patches/rcon-client+4.2.5.patch (patch-package postinstall; without it Conan RCON='Timeout for packet id 1'). CROSS-PLATFORM HARD RULE: never hardcode .exe/Win64/WindowsServer/backslash; use src/main/utils/platform.ts (getConanConfigSubfolder, getConanExecutableRelativePath, getSoulmaskExecutableRelativePath) + src/main/services/path-resolver.ts resolveServerPaths(baseDir,gameType)/predictServerPaths via PATH_CANDIDATES[gameType] (single source of all exe/gameDb/log/configDir paths; user picks ONE folder). Never surface raw filenames. THE TWO-ID SPLIT: serverId/communityServerId=Server._id (scopes /sync/* + /client/:id/*); gameInstanceId/apiServerId=GameInstance._id (WS handshake + RCON); the field NAMED apiServerId scopes the game-instance. Identity files: credentials.json PLAINTEXT (ACL = boundary) + host.json (fingerprint) under C:\\ProgramData\\Servitium (Win packaged) / $HOME/.config/Servitium (Linux). Orchestration: heartbeat 15s + reconcileServersFromApi + enforceDesiredState (RUNNING+dead→start, STOPPED+alive→stop, terminal states only, absent desiredState=never auto-start). MultiMonitoringManager Map<gameInstanceId,MonitoringManager>. MonitoringManager (~4922 lines) is the central game-type dispatcher — branch anchors: initializeDatabaseWatcher ~314 (Conan-only), initializeLogFileWatcher ~483 (SOULMASK→SoulmaskLogParser else ConanLogParser), initializeSyncOrchestrator ~779 (RestructuredSyncService Conan-only), forwardEvent building_destroy ~3114, deriveBaseDir ~126. Process managers src/main/services/process/: BaseServerProcessManager (detached, health 5min, quickCrash guard, preSpawnHook SQLite release); ConanExilesProcessManager (rcon-client, INI upsert); SoulmaskProcessManager (rcon-srcds, soc 1 chat-to-log, SteamAppId=2646460); GameProcessFactory switch with `never` exhaustiveness (compile error forces new case). Installers src/main/services/installers/ (Conan AppId 443030, Soulmask 3017310) + SteamCMDDownloader (+force_install_dir, exit 0/7/8 ok). Wipe src/main/commands/wipe-commands.ts: stop reader→wait 5s→isManual stop (mandatory else auto-restart re-locks game.db mid-wipe)→wait 10s; replace_game_db validates SQLite header + Conan tables; do NOT remove the hardcoded waits (Linux wipe race). Rust core src/rust-modules/game-db-reader/ (Conan SQLite ONLY, close() before wipe). WS contract src/shared/ws-events/gui.events.ts (mirrored in top-level shared/); UPDATE_AVAILABLE='soulmask:update-available' is used for BOTH games (misleading name). New game = NEW RustProcessManager (Rust RCON is Source-over-WebSocket, neither rcon-client nor rcon-srcds works) + RustInstaller (AppId 258550) + RustLogParser (log/RCON-only, NO SQLite — gameDbPath stays null) + factory cases + platform/path-resolver entries; NEVER route Rust through ConanDatabaseParser or the Rust reader.","servitium-discord":"Standalone Angular 20 SPA at discord.servitium.org, tsconfig FULL STRICT, Karma/Jasmine (ng test). The FREE, INTENTIONALLY agent-free + gameType-agnostic surface (welcome/goodbye embeds + Join-to-Create voice ONLY). No local stack: environment.apiUrl is hardcoded https://api.servitium.org for BOTH dev and prod, environment.production is ALWAYS false (do not branch on it; add a fileReplacements block first if you ever need a distinct prod value). Build @angular/build:application, always production config. i18n is a RUNTIME dict src/app/core/i18n/dicts.ts (DICTS, 6 langs, auto-extracted from Center) — NO build-time extraction; any new string MUST be added to all 6 langs there or it falls back to the raw key (prefer updating the Center extraction source so re-extraction doesn't clobber). Architecture = ports + adapters: 3 token bindings in src/app/app.config.ts (DISCORD_DATA_PORT→DiscordDataAdapter, TRANSLATE_PORT→I18nService, NOTIFICATION_PORT→NotificationService) are the ENTIRE wiring contract; the same @servitium/discord components run on Center. Auth: httpOnly-cookie Discord OAuth (token NEVER in localStorage; only display data under 'svt_discord_session'); authGuard async ensureAuthenticated() once per load. Two id spaces: Discord guildId (only /discord-communities/guild/:guildId/*) vs clientId = ManagedGuild.serverId (the Servitium Server id, used by ALL /clients/:clientId/discord/*; null until connect() — DashboardComponent.clientId returns '' then and adapter calls fail silently, always check state==='ready'). DiscordDataAdapter implements the FULL DiscordDataPort; the 5 agent/paid methods (refreshServerInfo, sendPvPStats, sendGuildPvPStats, sendBuildingLeaderboard, triggerBuildingAlerts) are present but NEVER called — RULE: any new DiscordDataPort method MUST be added here (even a stub) or the build breaks. SHARED LIB: after editing projects/servitium/discord/, REBUILD @servitium/discord tgz + re-link before changes show. Backend (servitium-api): discord-login.controller.ts + discord-community.controller.ts (JwtAuthGuard cookie) + discord-community.service.ts createDiscordCommunity (Server gameType=DISCORD tier='free'); env COOKIE_DOMAIN must be '.servitium.org' WITH leading dot (CRLF env gotcha). KNOWN bugs: discord-data.adapter.ts linkToExistingGuild URL has clientId twice; bot invite client-id fallback hardcoded '1424738313310568489'; stale serverId → loadConfig 401s silently; app.spec.ts is the stale scaffold (ng test currently FAILS); app.ts is bare <router-outlet/> with no toast host (toasts() has no renderer). This SPA itself does NOT branch on gameType; a new game adds at most a landing FeatureCard + paid channel keys in discord.types.ts (rebuild lib) — the agent log/RCON parser + API channel handler do the work.","servitium-autodev":"THIS engine. Private owner-only. tsconfig STRICT, CommonJS (package.json has NO type:module). TS 6.0.3, better-sqlite3 (WAL+FK), pino, octokit, @anthropic-ai/claude-agent-sdk@0.3.177 (PINNED — do not bump without approval + smoketest), zod, ws, jest+ts-jest. THE ESM RULE: every dynamic import of the SDK or octokit MUST go through importEsm<T> in src/esm.ts (new Function dodge); a bare import() throws ERR_REQUIRE_ESM. Two PM2 apps on 51.75.119.93: autodev-dashboard (dist/src/dashboard/server.js 256M) + autodev-orchestrator (dist/src/index.js 512M no-autorestart). Dedicated Console API key, ~100 EUR/mo cap. DB: ./autodev.db RELATIVE to cwd (process MUST run from repo root or it makes a second DB); SCHEMA_VERSION=3; TWO migration mechanisms — base-table change → BUMP SCHEMA_VERSION, additive column → ensureColumn() (never mix). spend_ledger.scope ∈ {'build','intel'} partitions the two cost lanes (a SIE write that forgets scope='intel' is charged to the build cap). BUILD lane: 17-state FSM (src/fsm/*), 10 agent roles (src/agents/*), deterministic gate matrix (src/gates/*; lint + coverage-aggregate are in the union but NOT registered), TCB scope guard (src/git/scopeGuard.ts — hard-excluded from agent-writable paths), bubblewrap sandbox on Linux, octokit DRAFT-PR-only (never merges/deploys). allowed_paths are HUMAN-confirmed at SPEC_APPROVAL (security boundary, never from LLM). NOTE buildExecutor SPEC_APPROVAL returns 'approved' UNCONDITIONALLY (the human gate is ASPIRATIONAL, not wired). INTEL lane (SIE, src/intel/*, separate 'intel' budget scope): scheduler (once per UTC day after SIE_HOUR_UTC=5) → pipeline PLAN→HARVEST→FETCH→EXTRACT→DEDUP→IDEATE→SCORE→TRANSLATE→BRIEF. THE GROUNDING: every prompt calls getActiveDossier() via ground(); set via setActiveDossier() from sie_kv 'dossier' (auto-refreshed by kb/refresh.ts — BUILT BUT NO SCHEDULER WIRED, run dist/scripts/refresh-context.js manually) else the hand-seeded SERVITIUM_DOSSIER constant (dossier.ts). owner_context (sie_kv) overrides the dossier, NO write UI. SLOTS map (pipeline.ts ~line 50, HARDCODED not from DB): games='Conan Exiles, Soulmask', candidateGames='Rust, Palworld, Enshrouded, V Rising, ARK', year='2026' (update annually). Scoring src/intel/score/rubric.ts: 8 axes, evidence-gating (uncited axis capped 0.3), learned bias ±0.08; flagship at >=85 + kind∈{game,business}. SIE_BRIEF_TOP_N=0 is the HUMAN GATE (do not change — prevents Opus firing autonomously); briefs are owner-greenlit-only, $3/pass $7 ceiling, 4 passes. The brief writes brief_md (FR) + max_prompt (paste-ready EN) + deeper_prompt. CONVENTIONS (src/agents/prompts.ts) is imported VERBATIM into the Max template (src/intel/brief/maxPromptTemplate.ts) — keep them in sync; it only states the API test rule (add per-repo deltas). verify.ts APP_TO_REPO maps api/center/portal/ui/electron-gui (MISSING discord+autodev). Dashboard: pure-Node http+WS+SSE, port 8787, cookie auth. THE PROMPT FILES TO EDIT for any engine improvement: dossier.ts (grounding), prompts.ts (all SIE agent prompts), brief/maxPromptTemplate.ts (the Max output), pipeline.ts (SLOTS + stage wiring). TEST: jest 30 + ts-jest (jest.config.cjs); mirror the house rule, ship a green test. Owner pushes git himself."};

export const NEW_GAME_PLAYBOOK: string = "## Definitive \"Add a New Game\" Playbook (worked for Rust)\n\nServitium is already built around a game abstraction. Adding a game = (1) extend the shared GameType enum, (2) add installer + process-manager + RCON + log parser in the AGENT, (3) register an RCON strategy + app-id + item scraper + map ranges in the API/Center, (4) wire frontends. Discord feeds are already game-agnostic at the channel level.\n\n### The 3 highest-risk Rust-specific gotchas (must not skip)\n1. RCON transport is WebSocket, not TCP. Rust uses Source-RCON-over-WebSocket. rcon-client (Conan) and rcon-srcds (Soulmask) both FAIL. You need a 3rd client lib (e.g. rustrcon / hand-rolled ws) and NEW branches in agent rust-process-manager.ts AND API rcon.service.ts / rcon/managers (add rust-rcon-ws.manager.ts).\n2. No SQLite game.db. Rust state is server/<identity>/*.sav, not a queryable DB. Rust must take the Soulmask-style log/RCON-only path: in monitoring-manager.ts skip the database watcher + RestructuredSyncService; in path-resolver.ts gameDbPath stays null. NEVER route Rust through ConanDatabaseParser or the Rust game_db_reader native module (it is hardcoded to the Conan schema).\n3. Config is server.cfg / +convar flags, not UE INI. Rust installer/process-manager must NOT use getConanConfigSubfolder()/WindowsServer/LinuxServer INI logic; launch args use +server.port / +rcon.web 1 syntax. Wipes DELETE .sav files, not swap game_0.db — add a Rust branch to the wipe orchestrator (game-db-sync/wipe.service.ts + agent wipe-commands).\n\n### Step 0 — shared/ (do FIRST)\n- shared/enums/game-type.enum.ts — add RUST='RUST' + GameTypeLabels entry. THE canonical enum (1 line propagates to all API chains).\n- servitium-electron-gui/src/shared/enums/game-type.enum.ts — DUPLICATE copy, keep in lockstep.\n- New event payload shapes go in shared/ws-events/*.payloads.ts.\n\n### Step 1 — servitium-electron-gui (the bulk of the work)\n- utils/platform.ts: add getRustExecutableRelativePath() (Linux RustDedicated, Win RustDedicated.exe). No ConfigSubfolder (no UE INI).\n- services/path-resolver.ts: add PATH_CANDIDATES[RUST] (exe RustDedicated; gameDb null; log stdout/console; configDir server/<identity>/cfg). gameDbPath null = the signal to skip the DB watcher.\n- installers/rust-installer.ts extends BaseGameInstaller: getSteamAppId()=258550 (RustDedicated), writeServerConfig() writes server.cfg. Add the case to game-installer-factory.ts + getSupportedGames/getGameDisplayName/getDefaultInstallPath.\n- process/rust-process-manager.ts (model on soulmask-process-manager.ts, log-only): getLaunchArguments() with +server.port/+rcon.port/+rcon.password/+rcon.web 1/+server.identity/+server.seed/+server.worldsize/+server.maxplayers/+server.hostname/+server.level \"Procedural Map\"; getEnvironmentVariables() (no SteamAppId env; maybe LD_LIBRARY_PATH/steamclient.so on Linux); RCON via WebSocket (initializeRconClient/testRconConnection/sendRconCommand/checkProcessHealth='serverinfo'/waitForServerReady=poll RCON/sendRconShutdown='quit').\n- process/game-process-factory.ts: add case GameType.RUST (the `never` default forces a compile error until you do). Default ports game 28015/rcon 28016/query 28017.\n- process/types.ts: add RustServerConfig + add to the GameServerConfig union.\n- parsers/rust-log-parser.ts (model on soulmask-log-parser.ts): emit the SAME event shape (player_connected/disconnected/chat_message/player_kill/building_destroy) so downstream is unchanged. Source = RCON serverinfo/playerlist polling + console log + Oxide plugins (NOT a file the way Conan's ConanSandbox.log works). Reuse the killCooldown anti-spam pattern.\n- monitoring-manager.ts (THE central dispatcher): every gameType==='SOULMASK'/'CONAN_EXILES' branch learns 'RUST'. logParser type adds | RustLogParser; the watcher-setup block adds a RUST branch (log/RCON path, skip DB watcher + RestructuredSyncService — already CONAN_EXILES-gated); the building_destroy raid-protection filter; audit every ... || 'CONAN_EXILES' fallback so Rust isn't coerced into Conan.\n- DO NOT touch src/rust-modules/game-db-reader/ (Conan-only; preserve it).\n\n### Step 2 — servitium-api\n- games.service.ts: add a 'rust' entry to the HARDCODED in-memory array (id/code/type/rconCommands/defaultPorts{game:28015,query:28017,rcon:28016}/features).\n- rcon/strategies/rcon-strategy.factory.ts: registerStrategy('rust', new RustRconStrategy()).\n- rcon/strategies/rust-rcon.strategy.ts implements IRconStrategy (model on conan/soulmask): Rust verbs playerlist, teleportpos/teleport2me, inventory.giveto \"{steamId}\" \"{itemId}\" {qty}, kick \"{steamId}\", ban \"{steamId}\", say \"{msg}\", server.save, quit; parsePlayerList for Rust status output; getDefaultPorts.\n- rcon.service.ts: add the WebSocket RCON branch (the single biggest API change). Audit rcon-maintenance.service.ts (hardcodes GameType.CONAN_EXILES in one spot).\n- soulmask-updates.service.ts GAME_APP_IDS: add 'rust':'258550'. game-updates.service.ts + controller: widen 'conan-exiles'|'soulmask' literals to add 'rust'.\n- game-items/schemas/game-item.schema.ts: add 'RUST' to gameType enum + a 'rust-itemlist' source; same literal-type widenings in game-items.service.ts. New scraper scripts/scrapers/scrape-rust-items.ts (technicalId = Rust item shortname e.g. rifle.ak or numeric itemid; icons from the Rust item DB / community CDNs); import-game-items.ts is game-agnostic.\n- client.service.ts: add RUST_WEAPON_MAP + a === GameType.RUST branch (Soulmask weapon-label is gated). Verify getServerGameType() cache (defaults CONAN_EXILES on miss).\n- protection-management.controller.ts + protection.service.ts: add a 'RUST' branch (Conan writes PVPBuildingDamage* INI + RCON; Soulmask uses soulmaskRaidTimeSyncService; Rust raiding = decay/server.cfg/Oxide — likely a no-op or plugin command).\n- game-db-sync/wipe.service.ts: add a Rust branch (delete server/<identity>/*.sav + map files, not swap game_0.db).\n- src/quests/* — NO change (zero gameType coupling; Rust quests work once player-stats/kill events flow).\n- map.service.ts — positions come from player-identity (filled by db-sync). For Rust, feed positions via RCON polling into the same store or the map stays empty. Coord transform is frontend-only.\n\n### Step 3 — Discord (already game-agnostic at the channel layer)\n- src/discord/default-channels.ts defines killfeed/pvp-stats/players-connection/violations/server-status etc.; discord-dispatch.service.ts routes by event type, not gameType. So once the Rust log/RCON parser emits the standard event types and they reach client.service.ts, the feeds light up for free. Check monitoring-discord.service.ts (prints gameType label — fine) and the disconnect resolver (Rust uses SteamID64).\n- servitium-discord (standalone) does NOT branch on gameType; add at most a landing FeatureCard + new paid channel keys in discord.types.ts (rebuild the @servitium/discord tgz).\n\n### Step 4 — Center\n- create-server-modal.component.ts: add {value:'RUST',label:'Rust'} to gameTypeOptions + widen the union.\n- map/services/coordinate-converter.service.ts: add a RUST branch with Rust world ranges (worldsize-dependent, centered at origin, single range — no Siptah split). Add Rust map tiles.\n- server-templates.component.ts: create Rust presets. Shop products: the generic game-item-picker works once game_items has RUST rows. soulmask.service.ts: create rust.service.ts only if Rust needs bespoke endpoints.\n- Other union widenings: server.service.ts L23, hosts.service.ts (DetectedServer.gameType + appId), game-items.service.ts, admin-gifts.component.ts L78, server-setup-wizard.component.ts, header.getGameIcon() L385 (add RUST:'hammer'); gameType-conditional tabs in players-list/parameters/features-layout/discord/wipe-management/host-scan; TitleService.routeTitles; config-general GameSpecificConfig.rust; i18n keys in all 6 files.\n\n### Step 5 — Portal (only ~4 files branch)\n- Server.model.ts: add 'rust' to the gameType union. CoordinateConverterService: add 'RUST' to SupportedGame + RUST_RANGE_X/Y + a RUST branch in gameToLeaflet/leafletToGame.\n- BountyMapComponent: add /assets/maps/rust-map.png + bounds + a third branch.\n- Leaderboard sub-components (stats-overview, pvp-leaderboard, guild-leaderboard): add isRust() to gate buildings/thralls columns and relabel.\n- SEO + banners: RUST arms in getGameTypeName()/serverImage()/server-browser gameTypeOptions/global-leaderboards (API lowercase 'rust')/profile; landing+about copy; /assets/og/og-rust.png + makeOg() in generate-og-images.mjs; all 6 messages.*.json + seed-faq-i18n.mjs. QuestMetric (quests.service.ts, portal-local) extend manually if API adds a metric.\n\n### Step 6 — servitium-ui (small)\n- Widen the gameType union duplicated across ~12 files + gameTypeChip/Label/getGameIcon switches + add a BEM SCSS rule per new variant. player-events-timeline event-type integers must branch for Rust or be driven by a [gameType]/[eventTypes] input. Rebuild npm run build:pack + reinstall the tgz in Center/Portal.\n\n### Step 7 — servitium-autodev (trivial)\n- Add the repo to TARGET_REPOS/CODE_REPOS (plain string, no schema change). GAME_APPID already maps rust:252490 in the dashboard. Update SLOTS.games/candidateGames in pipeline.ts + the dossier once Rust ships so the engine stops re-proposing it.\n\n### Steam App ID cheat-sheet\n| Game | Dedicated-server app id | Client app id (steam_appid.txt / env) | Workshop app id |\n| Conan Exiles | 443030 | n/a | 440900 |\n| Soulmask | 3017310 | 2646460 | — |\n| Rust (new) | 258550 (RustDedicated) | none needed | Oxide/uMod plugins (not Steam Workshop) |\n\n### Grep checklist before shipping (files that branch on gameType today)\nAgent: monitoring-manager.ts, game-process-factory.ts, game-installer-factory.ts, path-resolver.ts, platform.ts, process/types.ts, game-update-initializer.ts, game-update-handler.ts. API: games.service.ts, rcon-strategy.factory.ts, rcon.service.ts, rcon-maintenance.service.ts, soulmask-updates.service.ts, game-updates.service.ts, client.service.ts, protection-management.controller.ts, protection.service.ts, game-items.service.ts + schema, economy-stats.service.ts, admin.service.ts, bounty-hunt.service.ts, demo-seed.service.ts. Center: create-server-modal, coordinate-converter, server-templates, shop products, soulmask.service. Portal: Server.model, coordinate-converter, BountyMapComponent, leaderboard subs, SEO. Shared: game-type.enum.ts (canonical) + the electron-gui duplicate.";

export const FEATURE_CATALOG: string = "## Servitium complete feature catalog (functional → where it lives → new-game wiring)\n\n### servitium-api (the backend that owns all data + real-time flow)\n- Bootstrap (src/main.ts) — rawBody for Stripe, helmet, trust-proxy, IoAdapter, ValidationPipe, /health off guard chain, Swagger. Game-agnostic, reused as-is.\n- Auth (src/auth) — httpOnly-cookie JWT + Bearer, signSlim, Discord OAuth, refresh/verify/forgot, 5-attempt lockout. Game-agnostic.\n- Users/Team/Roles/Invitations/Admin (src/users, src/team, src/server-roles, src/permission-templates, src/admin-invitations, src/admin) — User schema (gameAccounts[{id,platform,isPrimary}]), ServerRole OWNER|ADMIN + PermissionSet, 5 DEFAULT_TEMPLATES, SUPERADMIN cascade. Game-agnostic.\n- Dashboard (src/users/dashboard.*) — parallel metrics/economy/protection/ddos/registrations/donations aggregate + action items + deltas. Game-agnostic if agent populates game_players.\n- Servers (src/servers) — Server CRUD, FREE_SERVER_LIMIT=1, seed chain on create (~L157-213), public portal endpoints, heartbeat→runtime→WS, splitForUpdate via RUNTIME_FIELD_NAMES. New game: add seed branch.\n- GameInstances (src/game-instances) — runtime doc, control/install/detect-paths/config-file/uninstall/forceKill/reboot via GuiGateway, assertNoPortConflict. gameMode PVP|PVE|PVE-C may need extending for Rust.\n- Hosts (src/hosts) — Host fingerprint, registerOrUpdate, heartbeat-claims-scan-jobs, filesystem-scan wizard. Game-agnostic.\n- Registration (src/registration) — links portal player to Steam(17-digit)/Epic(32-hex), auto-complete or 8-char/10min code, creates wallet, assigns quests. Game-agnostic (Rust=Steam).\n- Server-templates (src/server-templates) — BUILTIN_PRESETS + snapshots, gameType match enforced. New game: add Rust presets.\n- Config-general (src/config-general) — per-game multipliers/flags (conanExiles typed, soulmask freeform), live-apply Conan via RCON. New game: add typed gameSpecific.rust + updateRustConfig().\n- Server-scheduler (src/server-scheduler) — @Cron(EVERY_MINUTE) daily/beforeRaid/afterRaid restarts, timezone RCON warnings, physical restart. New game: branch sendWarning()/executeScheduledRestart().\n- Server-status/reachability (src/servers) — @Interval(60s) offline at >180s, RconReachability circuit breaker. Game-agnostic.\n- Client-agent intake (src/client) — register/heartbeat/metrics/events/game-events/batch; routes events to stats/guilds/shop/decay/war/raid/bounty/quest; weapon map Soulmask-gated. New game: add weapon map + watched-items + kill field aliases. AUTH GAP: 2 endpoints accept any Bearer.\n- Client-downloads (src/client-downloads) — ZERO-auth versioned binary/script serving. Game-agnostic.\n- Game-db-sync orchestrator + wipe + backups + guilds (src/game-db-sync) — light/full/batch into 6 collections, auto-ban, bounty positions, DDoS auto-whitelist, snake→camel; KILL=eventType 103 HARDCODED ~L1181 (primary new-game branch point); wipe 6-step (@Cron */1); templates game_db_templates (enum conan-exiles|soulmask). New game: per-game kill-type map + 'rust' template enum + wipe-deletes-.sav branch.\n- Players-identity + stats + bans + anti-cheat (src/players-identity) — paginated list (online/IP-dup/VPN(IPQualityScore)/sessions), RCON ban→player_bans, BanExpiration @Cron, leaderboards @Public. New game: RCON ban syntax + GameEventType constants.\n- Chat (src/chat, src/chat-logs) — chat_logs (Pro-gated Entitlement.Chat), connection events→Discord, !buy/!register/!rp reroute. Game-agnostic; parseChatMessage may need a Rust branch.\n- Game-events / player-events (src/game-events, src/player-events) — raw string-type vs numeric-type; TWO systems (game_events_raw string GameEventType vs game_events numeric eventType in event-types.constants.ts). New game: add Rust numeric constants.\n- Live map (src/map, MapGateway /ws/map) — player/guild/building/violation/chat to map:{serverId}. Conan region resolver is the only game-specific bit.\n- Economy (src/domain/economy) — per-server 3-bucket wallet (balance resets on wipe, donationBalance permanent), shop (ITEM/KIT/WILDCARD/RAID_ALERT), PaycheckService @Cron, DonationService(PayPal), cashback/boost/freeze, atomicTransfer. New game: GameItem.gameType enum + RCON give command.\n- Economy-events + Admin-gifts + Game-items (src/economy-events, src/admin-gifts, src/game-items) — bulk GIFT/PROMOTION/BONUS, RCON sendGiftBundle (online-or-queue), game_items canonical catalogue + self-hosted icons. New game: add gameType to GameItem enum + scraper + 'rust-rcon'/'rust-itemlist' source.\n- Quests (src/quests) — daily/weekly, live-computed progress, auto-claim, Pro-gated Entitlement.FeatureQuests. ZERO gameType coupling — works for Rust automatically (only enemyThrallsKilled is Conan-only).\n- Bounty-hunt (src/bounty-hunt) — @Cron weighted target, RCON+Discord announce, fast-track position loop, Pro-gated. New game: RCON gate branch + guild resolution.\n- Raid-protection (src/protection) — immunity via !rp/portal, violation detection, RCON prison teleport, config snapshot, @Cron. New game: RCON teleport strategy + agent violation payload + a 'RUST' branch in protection.service.ts (Conan/Soulmask branched).\n- Wars (src/wars) — portal clan-war in pre-raid window, weekly limit, WAR_BREACH, Pro-gated. Game-agnostic data model.\n- Raid-alerts (src/raid-alerts) — shop/owner subscriptions, fan-out push(VAPID)/Discord-DM/portal-WS, 10min debounce, Pro-gated. Game-agnostic.\n- Tickets (src/tickets) — report/tech/shop/raid tickets, per-ticket Discord channel, quick actions, RP gift buttons. Game-agnostic.\n- RCON subsystem (src/rcon) — connection pool, Conan(rcon-client)/Soulmask(rcon-srcds+interactive) routing, raw SQL on Conan game.db, @05:00 maintenance, the rcon-client patch, Pro-gated Entitlement.Rcon. New game: rust-rcon.strategy.ts + rust-rcon-ws.manager.ts (WebSocket!) + branch rcon.service.ts.\n- Rcon-chat (src/rcon-chat) — read chat_logs, broadcast, start/stop/restart via GuiGateway. Game-agnostic.\n- Soulmask automation (src/soulmask) — broadcast/player-sync/save-world/raid-time/portal crons (NO auth guard on controller). New game: parallel src/rust/.\n- Game-updates (src/game-updates, src/soulmask-updates) — Steam build-id poll @5min (Conan 443030, Soulmask 3017310 via GAME_APP_IDS), GuiEvents.UPDATE_AVAILABLE. New game: add appId 258550 + widen 'conan-exiles'|'soulmask' unions to 'rust'.\n- Discord bot core + channel services (src/discord) — guild link/unlink, per-channel toggles, cron refresh, dispatch (killfeed/connection/bounty), community onboarding (DISCORD-tier), 20+ channel-feature services (status voice, monitoring embed, leaderboards, banners, tickets, raid display, war board, wipe announcements). Mostly game-agnostic (fed by agent). New game: GameType to label map + channel handler branch.\n- Billing (src/billing) — Stripe Checkout (single STRIPE_PRICE_ID) flips Server.plan, webhook @Public + rawBody. Game-agnostic.\n- Subscriptions (src/subscriptions) — LEGACY user-level admin CRUD, NO Stripe link, parallel to entitlements. DEAD-ish.\n- Entitlements (src/entitlements + shared/entitlements) — Server.plan gate, 27 keys all 'pro', EntitlementGuard. New game: add keys only if a Rust-specific feature has no analog.\n- Payments (src/infrastructure/payments/paypal.service.ts) — PayPal v2 orders for player DONATIONS only. Game-agnostic.\n- DDoS (src/ddos) — per-server blacklist/whitelist, auto-blacklist, OPNsense alias sync (IP_WHITELIST_CONAN/SOULMASK), Pro-gated SubscriptionFeature.DDOS_PROTECTION (legacy). New game: IP_WHITELIST_RUST alias.\n- Notifications/Announcements/Banners/Contact/Mail/Audit/Sitemap (src/notifications, src/announcements, src/banners, src/contact, src/mail, src/email, src/audit, src/sitemap) — in-app alerts, event feed, hero/thumb WebP reencode (sharp), TWO email stacks, append-only audit (180d retention), dynamic XML sitemap (6 langs). Game-agnostic (sitemap: add Rust portal subpages).\n- WS gateways + infra (src/infrastructure/websocket, src/infrastructure) — GuiGateway /ws/gui (agent, room=gameInstanceId, sendCommandToGui), AdminGateway /ws, MapGateway /ws/map, PublicGateway /public; @Global CacheService(Redis), QueueService(BullMQ, no workers wired), TimezoneService. Game-agnostic (thrall-spawn handler Conan-specific).\n- Demo (src/demo) — seeds CONAN_EXILES dataset, 03:00 reset, DemoReadOnlyGuard. New game: parameterize for a Rust demo.\n- Migrations (src/migrations) — idempotent boot-time one-shots, failures swallowed. Game-agnostic.\n\n### servitium-center (admin control plane)\nBootstrap + shell layout (header live counts, sidebar role/feature-flag filtered) + dashboard (single aggregate + WS live counts + onboarding wizard) + auth area + guards + 3 interceptors; economy/shop (7 tabs) + GameItemPicker + SoulmaskService; players roster (kick/ban/teleport-clipboard/give-lead-guild Conan-only/prison-tp/clan-history Soulmask-only/fly-mode); DDoS/protection hub; raid-protection feature (3-step wizard); tickets; discord (per-channel toggle/category drag-drop/RP gift picker); parameters hub (general/economy/conan-or-soulmask/raid/scheduler/backups); features admin (bounty/quests/events/raid-alerts; treasure+boss-hunt Soulmask stubs); wars; live map (Leaflet CRS.Simple, tile pyramid Conan / image overlay Soulmask, force-sync); team + audit + rcon-chat + wipe-management (templates max 5/50MB) + install-agent + upgrade(checkout '9,99 €'); super-admin /admin/* (users, server-plans flip, discord-deployments, ip-management, stats-tools Conan-filter, demo-sessions); hosts-management /admin/hosts/* (list/detail/scan/setup-wizard). New-game branches: ~7 union widenings + getGameIcon() RUST:'hammer' + gameType-conditional tabs + CoordinateConverter RUST arm + 6 locale files.\n\n### servitium-portal (player site)\nBootstrap/SSR; routing (childRoutes spread 6x); auth (cookie + in-memory); server context + guards; two WS namespaces; server browser; global + per-server leaderboards; server-home dashboard; statistics page; shop (Soulmask npc-presets); donations (PayPal); raid protection (!rp); bounty hunt (Leaflet map); war declaration; quests (QuestMetric portal-local); wallet (cashback withdraw, cross-server donation transfer, 2-path gameAccountId change); events/announcements; online players; my-stats; alert notifications (Web Push/VAPID + Discord-link OAuth); registration handshake (!register); profile; i18n (6 langs); SEO (canonical/hreflang/OG/JSON-LD); analytics (GA4 Consent Mode v2); portal-local UI set (22 components). New-game branches: ~4 files (Server.model union, CoordinateConverterService, BountyMapComponent, leaderboard signals) + SEO/banner copy + OG image + 6 locale files.\n\n### servitium-ui (design system + Discord lib)\n@servitium/ui atoms (form + display + svt-icon + tooltip) + 4 chart atoms + molecules (alert/modal/notification/breadcrumb/data-table/dropdown/empty-state/page-header/tabs/accordion/stats-card/chat-feed/chat-messages-timeline/player-events-timeline/table-filters/portal-hero/portal-server-grid + non-exported cards) + 8 admin organisms (not exported); @servitium/discord (3 components + 3 ports + types). Mostly game-agnostic. New-game branches: the gameType union duplicated across ~12 files + gameTypeChip/Label/getGameIcon switches + player-events-timeline event-type integers + a BEM SCSS rule per new variant.\n\n### servitium-electron-gui (the agent)\nApp orchestrator + --register CLI + AuthManager + HostManager + MultiMonitoringManager + MonitoringManager (~4922 lines, the central dispatcher) + heartbeat/reconcile/enforceDesiredState; config/api-client/system trackers; process managers (Conan rcon-client / Soulmask rcon-srcds / Base) + GameProcessFactory (never-exhaustive) + installers + SteamCMDDownloader; wipe/server-control/host commands; feature services (Bounty/GameUpdate/Restart/Autostart); parsers (ConanLogParser, SoulmaskLogParser, ConanDatabaseParser, ServerCommandLogParser); sync (RestructuredSyncService active Conan-only + BatchSync + PlayerIpSync real-time; a dead trio); Rust game_db_reader; SystemTracker; server-scanner; FirewallManager (Windows); PortOrchestrator + SecurePortGenerator; SubscriptionService (the ONLY agent gate); config-file editor; setup-client.ps1/.sh. New-game: RustProcessManager (WebSocket RCON) + RustInstaller(258550) + RustLogParser (no SQLite) + factory cases + platform/path-resolver entries + GAME_EXES/GAME_APP_IDS + wipe deletes .sav.\n\n### servitium-discord (free Discord product)\nOAuth login (cookie) + SessionService + authGuard/interceptor + CommunityService (onboarding) + guild picker + bot invite + free community creation + welcome/goodbye editors + voice hubs + dashboard state machine + DiscordDataAdapter (full port, 5 agent stubs) + I18nService (runtime dict) + NotificationService + LandingComponent. All game-agnostic — a new game adds at most a landing card + paid channel keys in discord.types.ts (rebuild lib); the parser/handler work is agent+API side.\n\n### servitium-autodev (this engine)\nBUILD lane (poll/processor/runTask, 17-state FSM, 10 roles, gate matrix, TCB scope guard, MCP tools + hooks, bubblewrap sandbox, cost ledger, GitHub draft-PR composer, ESM bridge); INTEL lane (veille pipeline, 7 sensing angles, 8-axis scoring + ranking, Opus feasibility brief + Max prompt template, learning loop, KB/dossier refresh, code-scan lane, post-ship integration verifier, deep research reports); owner dashboard (http+WS+SSE, opportunity lifecycle, brief/verify/report/veille/code-scan triggers, logbook, Steam-banner display) + scripts + PM2. New game: add the repo to TARGET_REPOS/CODE_REPOS (string, no schema change); GAME_APPID already maps rust:252490 in the dashboard.";

export const TARGET_APP_ENUM: readonly string[] = ["servitium-api","servitium-center","servitium-portal","servitium-ui","servitium-electron-gui","servitium-discord","servitium-autodev","new-app"];
